import { act } from "react-test-renderer";

export function reducer(state,action){
    switch (action.type) {
        case value:
            
            break;
    
        default:
            return state
    }
}